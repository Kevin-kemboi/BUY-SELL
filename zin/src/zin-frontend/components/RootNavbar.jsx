import GlobalSearch from "./GlobalSearch";

const RootNavbar = () => {
  return <nav className="bg-dark-6  py-1 pt-2 w-full flex items-center justify-center">
    <GlobalSearch/>
  </nav>;
};

export default RootNavbar;
